"use client";

import { useEffect, useRef, useState } from "react";
import Link from "next/link";
import { supabase } from "@/lib/supabase/client";
import { Bell, Check, CheckCheck, Clock, MessageSquareText, XCircle } from "lucide-react";

interface Notification { id:string; pr_no:string|null; type:"info"|"update"|"remark"|"rejected"; title:string; message:string; is_read:boolean; created_at:string; }

export function NotificationPopover(){
 const [open,setOpen]=useState(false); const [items,setItems]=useState<Notification[]>([]); const [loading,setLoading]=useState(false); const [error,setError]=useState(false); const ref=useRef<HTMLDivElement>(null);
 const load=async()=>{try{setLoading(true);setError(false);const {data:{session}}=await supabase.auth.getSession();const uid=session?.user?.id;if(!uid){setItems([]);return}const {data,error}=await supabase.from("notifications").select("id,pr_no,type,title,message,is_read,created_at").eq("user_id",uid).order("created_at",{ascending:false}).limit(8);if(error){console.warn("Notification load failed:",error.message);setError(true);return}setItems((data||[]) as Notification[])}catch(err){console.warn("Notification load failed:",err);setError(true)}finally{setLoading(false)}};
 useEffect(()=>{if(!open)return;load();const interval=window.setInterval(load,30000);return()=>window.clearInterval(interval)},[open]);
 useEffect(()=>{const close=(event:MouseEvent)=>{if(ref.current&&!ref.current.contains(event.target as Node))setOpen(false)};document.addEventListener("mousedown",close);return()=>document.removeEventListener("mousedown",close)},[]);
 const unread=items.filter(i=>!i.is_read).length;
 const markRead=async(id:string)=>{await supabase.from("notifications").update({is_read:true}).eq("id",id);setItems(c=>c.map(i=>i.id===id?{...i,is_read:true}:i))};
 const markAll=async()=>{const {data:{session}}=await supabase.auth.getSession();const uid=session?.user?.id;if(!uid||!unread)return;await supabase.from("notifications").update({is_read:true}).eq("user_id",uid).eq("is_read",false);setItems(c=>c.map(i=>({...i,is_read:true})))};
 const iconFor=(type:Notification["type"])=>type==="rejected"?<XCircle className="h-4 w-4"/>:type==="remark"?<MessageSquareText className="h-4 w-4"/>:<Bell className="h-4 w-4"/>;
 return <div ref={ref} className="relative"><button type="button" onClick={()=>setOpen(v=>!v)} className="relative rounded-xl p-2 text-stone-500 transition hover:bg-[#FDF2F7] hover:text-[#7B0046]" aria-label={`Notifications${unread?`, ${unread} unread`:""}`} aria-expanded={open}><Bell className="h-4 w-4"/>{unread>0&&<span className="absolute right-1.5 top-1.5 h-1.5 w-1.5 rounded-full bg-[#F5AB26]"/>}</button>
 {open&&<div className="absolute right-0 top-[calc(100%+10px)] z-[70] w-[min(360px,calc(100vw-1.5rem))] overflow-hidden rounded-2xl border border-[#7B0046]/10 bg-white shadow-[0_20px_60px_rgba(45,0,26,.16)] sm:w-[360px]"><div className="flex items-center justify-between border-b border-stone-100 px-4 py-3"><div><p className="text-[11px] font-extrabold text-[#4D002C]">Notifications</p><p className="mt-0.5 text-[8px] text-stone-400">Procurement updates and remarks</p></div><button type="button" onClick={markAll} disabled={!unread} className="inline-flex items-center gap-1 rounded-lg px-2 py-1.5 text-[8px] font-bold text-[#7B0046] hover:bg-[#FDF2F7] disabled:opacity-35"><CheckCheck className="h-3 w-3"/>Mark all read</button></div><div className="max-h-[min(390px,65vh)] overflow-y-auto">{loading&&items.length===0?<div className="px-5 py-10 text-center text-[9px] text-stone-400">Loading notifications…</div>:error?<div className="px-5 py-9 text-center"><Bell className="mx-auto h-7 w-7 text-stone-300"/><p className="mt-2 text-[10px] font-bold text-stone-700">Notifications unavailable</p><button onClick={load} className="mt-2 text-[8px] font-bold text-[#7B0046] hover:underline">Try again</button></div>:items.length===0?<div className="px-5 py-10 text-center"><Bell className="mx-auto h-7 w-7 text-stone-300"/><p className="mt-2 text-[10px] font-bold text-stone-700">No notifications yet</p><p className="mt-1 text-[8px] leading-4 text-stone-400">New procurement updates will appear here.</p></div>:items.map(item=><div key={item.id} className={`border-b border-stone-100 px-4 py-3 ${item.is_read?"bg-white":"bg-[#FFFDF7]"}`}><div className="flex gap-2.5"><div className={`flex h-8 w-8 shrink-0 items-center justify-center rounded-lg border ${item.type==="rejected"?"border-red-100 bg-red-50 text-red-600":item.type==="remark"?"border-amber-100 bg-amber-50 text-amber-700":"border-[#7B0046]/10 bg-[#FDF2F7] text-[#7B0046]"}`}>{iconFor(item.type)}</div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-2"><p className="text-[9px] font-extrabold text-stone-800">{item.title}</p>{!item.is_read&&<span className="rounded-full bg-[#7B0046] px-1.5 py-0.5 text-[6px] font-black uppercase tracking-wide text-white">New</span>}</div><p className="mt-1 text-[8px] leading-4 text-stone-500">{item.message}</p><div className="mt-1.5 flex items-center justify-between gap-2"><span className="inline-flex items-center gap-1 text-[7px] text-stone-400"><Clock className="h-2.5 w-2.5"/>{new Date(item.created_at).toLocaleString()}</span><div className="flex items-center gap-2">{item.pr_no&&<Link href={`/dashboard/pr/${item.pr_no}`} onClick={()=>{if(!item.is_read)markRead(item.id);setOpen(false)}} className="text-[7px] font-bold text-[#7B0046] hover:underline">View PR</Link>}{!item.is_read&&<button type="button" onClick={()=>markRead(item.id)} className="inline-flex items-center gap-1 text-[7px] font-bold text-stone-400 hover:text-[#7B0046]"><Check className="h-2.5 w-2.5"/>Read</button>}</div></div></div></div></div>)}</div></div>}
 </div>;
}
